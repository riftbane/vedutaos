# The demo games every release archive and every image carries, besides the engine's own
# demo (its template, at the engine version go.mod requires): name and tag of each repository
# under github.com/riftbane, built for the console by vedutaos from the tagged source.
TINYCUBE_TAG=v0.1.0
