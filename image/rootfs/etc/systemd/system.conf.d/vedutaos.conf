# Under emulation the first boot brings udev up slowly, and systemd's usual 90 s would give
# up on the boot partition and the serial console before it does. A Pi never needs the room.
[Manager]
DefaultDeviceTimeoutSec=600s
DefaultTimeoutStartSec=600s
